{
  "name": "memory-match",
  "version": "1.0.0",
  "description": "A Memory Match game with global leaderboards",
  "scripts": {
    "start": "serve . -p ${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": "18.x"
  }
}
